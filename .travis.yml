language: perl
os: linux
sudo: false
perl:
  - "5.26"
  - "5.24"
  - "5.20"
env:
  global:
    # some dependencies might require this :(
    - PERL_USE_UNSAFE_INC=1
script:
  # install all dependencies
  - cpanm --installdeps --develop .
  # build and test this dist
  - dzil test --all
  # test installation
  - dzil install
  # ok great thanks :)
